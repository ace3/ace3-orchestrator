POSTGRES_PASSWORD=mp_dev_password
POSTGRES_PORT=5432

MP_DB_DSN=postgres://mp:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/mini_paperclip?sslmode=disable
MP_PORT=8081
MP_API_TOKEN=dev-token
MP_HEARTBEAT_INTERVAL=60s
MP_WORKERS=4
MP_MAX_TASKS_PER_HEARTBEAT=10
MP_CLI_TIMEOUT=600s
MP_RUN_MAX_USD=1.00
MP_MONTH_MAX_USD=100.00
MP_RUNNER_MODE=mock
MP_REPO_ALLOWLIST=${HOME}
MP_SKILLS_CACHE_DIR=${PWD}/backend/tmp/skills-cache
MP_WORKTREES_DIR=${PWD}/backend/tmp/worktrees
